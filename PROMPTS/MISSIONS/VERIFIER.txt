PASS/FAIL only, then up to 6 bullets of evidence.

1) Static grep must show:
- resampleRatio = sourceRate / TARGET_SAMPLE_RATE
- destBlockSize computed using ceil(CHUNK_SIZE / resampleRatio) (plus margin)
- clamp exists: if numOutputSamples > resampledBuffer.getNumSamples() then clamp

2) Build Debug and run REAPER from Terminal.
3) Insert VoxScript and trigger extraction once.
4) Confirm no "malloc: Incorrect checksum" lines.