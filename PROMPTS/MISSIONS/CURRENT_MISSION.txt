Return only the final modified files.
No markdown outside required per-file code blocks.
C++17, JUCE, macOS.

TASK:
Add VOXSCRIPT_NO_LOGGING=1 env var that disables all VoxScript file logging and “debug session” initialization during plugin construction. Console stderr prints may remain.

FILES TO MODIFY:
- Wherever the VoxScript log session is created/opened (the code that prints “VoxScript Debug Session” and writes the log file path)
- Source/PluginProcessor.cpp (only if logging is initialized there)

REQUIREMENTS:
1) If getenv("VOXSCRIPT_NO_LOGGING") == "1":
   - Do not create log directory
   - Do not open/write any log files
   - Do not start “Debug Session” blocks
   - Do not allocate logger singletons
2) Keep minimal stderr prints like “[VoxScript] createPluginFilter() called”.

DELIVERABLE:
Provide final content of modified files only.